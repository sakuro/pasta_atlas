<% if avatar_url %>
  <img src="<%= avatar_url %>" width="<%= avatar_size %>" height="<%= avatar_size %>" style="border-radius:50%">
<% else %>
  <i class="fa-solid fa-circle-user" style="font-size:<%= avatar_size %>px"></i>
<% end %>
