<section class="hero">
  <%= image_tag "hero.webp", alt: "Pasta Atlas", style: "width: 100%; height: 360px; object-fit: cover; display: block;" %>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-multiline">
      <% maps.each do |map| %>
        <% user = users_by_id[map.user_id] %>
        <div class="column is-half-tablet is-one-quarter-desktop">
          <%= render "map_card",
                map_ulid: map.ulid,
                map_display_name: map.display_name,
                user_name: user.name,
                author_display_name: profiles_by_user_id[user.id].display_name || user.name,
                author_avatar_url: avatar_urls_by_user_id[user.id],
                thumbnail_url: thumbnail_urls_by_map_ulid[map.ulid] %>
        </div>
      <% end %>
    </div>

    <nav class="pagination is-centered mt-5" role="navigation" aria-label="pagination">
      <% if page > 1 %>
        <a class="pagination-previous" href="/?page=<%= page - 1 %>">Previous</a>
      <% end %>
      <% if page * per_page < total %>
        <a class="pagination-next" href="/?page=<%= page + 1 %>">Next</a>
      <% end %>
    </nav>
  </div>
</section>
